type Query {
  orders: [Order!]
  getLatestOrder: Order
}

type Order {
  id: ID!
  total: Float!
  discount: Float!
  net: Float!
  customer: Customer
  user: User!
  status: OrderStatus!
  paymentMode: PaymentMode
  items: [Item!]!
}
